<header>
    <nav class="navbar">
        <div class="container">
            <a href="/" class="logo">Tusenfryd</a>
            <div class="nav-links">
                <a href="/attractions">Attractions</a>
                <% if (typeof user !== 'undefined' && user) { %>
                    <a href="/my-reservations">My Reservations</a>
                    <% if (user.isAdmin) { %>
                        <a href="/admin">Admin Dashboard</a>
                    <% } %>
                    <a href="/logout" class="btn small-btn">Logout</a>
                <% } else { %>
                    <a href="/login">Login</a>
                    <a href="/register" class="btn small-btn">Register</a>
                <% } %>
            </div>
            <div class="menu-toggle">
                <i class="fas fa-bars"></i>
            </div>
        </div>
    </nav>
</header>
